#%RAML 1.0
title: Pick Slips
version: v0.2
protocols: [ HTTP, HTTPS ]
baseUri: http://localhost:9130

documentation:
  - title: API for fetching items requiring actual cost
    content: <b>API for items requiring actual cost generation</b>

types:
  loan: !include loan.json
  items-lost-requiring-actual-costs: !include items-lost-requiring-actual-costs.json

traits:
  language: !include raml-util/traits/language.raml

resourceTypes:
  collection-get: !include raml-util/rtypes/collection-get.raml

/inventory-reports:
  /items-lost-requiring-actual-costs:
    type:
      collection-get:
        exampleCollection: !include examples/items-lost-requiring-actual-costs.json
        schemaCollection: items-lost-requiring-actual-costs
