stages:
  - name: Build
    steps:
      - runScriptConfig:
          image: maven:3-openjdk-8
          shellScript: mvn clean install -DskipTests
  - name: Build Docker
    steps:
      - publishImageConfig:
          dockerfilePath: ./Dockerfile
          buildContext: .
          tag: docker.dev.folio.org/mod-circulation:core-functional-latest
          pushRemote: true
          registry: docker.dev.folio.org
timeout: 60
notification: {}
